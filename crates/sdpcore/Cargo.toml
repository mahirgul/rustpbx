[package]
name = "sdpcore"
version = "0.1.0"
edition = "2021"
description = "Pure Rust zero-I/O SDP parser and negotiation library (WASM compatible)"

[dependencies]
bytes = { workspace = true }
nom = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }
